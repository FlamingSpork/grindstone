[project]
name = "grindstone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "numpy>=2.3.5",
    "pandas>=3.0.1",
    "pillow>=12.0.0",
    "pynmeagps>=1.1.2",
    "scipy>=1.16.3",
]

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = [
    "src/"
]

